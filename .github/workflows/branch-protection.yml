name: 🛡️ Branch Protection

on:
  pull_request:
    branches: [main]

jobs:
  validate-source-branch:
    name: Validate PR Source Branch
    runs-on: ubuntu-latest
    
    steps:
      - name: Check Branch Protection Rules
        run: |
          echo "🔍 Validating PR source branch..."
          echo "📍 Source: ${{ github.head_ref }}"
          echo "🎯 Target: ${{ github.base_ref }}"
          echo ""
          
          SOURCE_BRANCH="${{ github.head_ref }}"
          
          # Define allowed patterns
          if [[ "$SOURCE_BRANCH" == "develop" ]] || 
             [[ "$SOURCE_BRANCH" =~ ^hotfix/.+ ]] || 
             [[ "$SOURCE_BRANCH" =~ ^release/.+ ]]; then
            echo "✅ SUCCESS: Branch '$SOURCE_BRANCH' is authorized to merge to main"
            echo "🎉 This PR follows our branching policy and can proceed"
          else
            echo "🚫 POLICY VIOLATION: Branch '$SOURCE_BRANCH' cannot merge to main"
            echo ""
            echo "📋 ALLOWED SOURCE BRANCHES:"
            echo "   • develop           (regular releases)"
            echo "   • hotfix/*          (critical fixes)"  
            echo "   • release/*         (release preparation)"
            echo ""
            echo "🔄 CORRECT WORKFLOW:"
            echo "   $SOURCE_BRANCH → develop → main"
            echo ""
            echo "💡 NEXT STEPS:"
            echo "   1. Close this PR"
            echo "   2. Create PR: $SOURCE_BRANCH → develop"
            echo "   3. After merge to develop, create: develop → main"
            echo ""
            echo "This check failed to enforce our branching policy."
            exit 1
          fi
